'use strict';

describe('myApp.aubourg.presentation module', function() {

  beforeEach(module('myApp.aubourg.presentation'));

  describe('presentation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var presCtrl = $controller('presCtrl');
      expect(presCtrl).toBeDefined();
    }));

  });
});