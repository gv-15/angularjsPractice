'use strict';

describe('myApp.valle.cv module', function() {

  beforeEach(module('myApp.valle.cv'));

  describe('cv controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cvCtrl = $controller('cvCtrl');
      expect(cvCtrl).toBeDefined();
    }));

  });
});