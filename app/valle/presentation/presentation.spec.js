'use strict';

describe('myApp.valle.presentation module', function() {

  beforeEach(module('myApp.valle.presentation'));

  describe('presentation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var presCtrl = $controller('presCtrl');
      expect(presCtrl).toBeDefined();
    }));

  });
});
