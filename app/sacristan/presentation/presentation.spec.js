'use strict';

describe('myApp.sacristan.presentation module', function() {

  beforeEach(module('myApp.sacristan.presentation'));

  describe('presentation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var presCtrl = $controller('presCtrl');
      expect(presCtrl).toBeDefined();
    }));

  });
});